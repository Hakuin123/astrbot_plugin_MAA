{
  "http_host": {
    "type": "string",
    "default": "0.0.0.0",
    "description": "HTTP 服务监听地址",
    "hint": "MAA 将通过此地址连接插件"
  },
  "http_port": {
    "type": "int",
    "default": 2828,
    "description": "HTTP 服务端口",
    "hint": "确保端口未被占用，MAA 需要配置相同端口"
  },
  "auto_screenshot": {
    "type": "bool",
    "default": true,
    "description": "任务完成后自动截图",
    "hint": "开启后每次任务完成时会自动附加一个截图任务"
  },
  "notify_on_each_task": {
    "type": "bool",
    "default": false,
    "description": "每项任务完成后立即发送通知",
    "hint": "关闭时仅在所有任务全部完成后发送一次通知（默认）；开启后每完成一个任务都会发送通知"
  }
}